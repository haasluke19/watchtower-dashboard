"use client";
import {useEffect,useState} from "react";
const priorities=["critical","high","normal","low"];
export default function Targets(){const [q,setQ]=useState("");const [priority,setPriority]=useState("");const [page,setPage]=useState(1);const [data,setData]=useState<any>({items:[],pages:1,total:0});const [busy,setBusy]=useState("");async function load(){const r=await fetch(`/api/targets?q=${encodeURIComponent(q)}&priority=${priority}&page=${page}`,{cache:"no-store"});if(r.ok)setData(await r.json())}useEffect(()=>{const t=setTimeout(load,200);return()=>clearTimeout(t)},[q,priority,page]);async function change(uuid:string,p:string){setBusy(uuid);await fetch("/api/commands",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({type:"set_priority",uuid,priority:p})});setBusy("");setTimeout(load,35000)}return <div className="stack"><section className="hero compact"><div><div className="eyebrow">CORPUS</div><h1>Targets</h1><p>Search the synced target set and queue priority changes for the VPS.</p></div><div className="counter">{data.total.toLocaleString()} results</div></section><div className="filters"><input value={q} onChange={e=>{setQ(e.target.value);setPage(1)}} placeholder="Search label or current name…"/><select value={priority} onChange={e=>{setPriority(e.target.value);setPage(1)}}><option value="">All priorities</option>{priorities.map(p=><option key={p}>{p}</option>)}</select></div><div className="panel tablePanel"><table><thead><tr><th>Target</th><th>Current name</th><th>Priority</th><th>Last checked</th><th>Failures</th></tr></thead><tbody>{data.items.map((t:any)=><tr key={t.uuid}><td><strong>{t.label}</strong><small>{t.uuid}</small></td><td>{t.current_name||"—"}</td><td><select className={`priority ${t.priority}`} value={t.priority} disabled={busy===t.uuid} onChange={e=>change(t.uuid,e.target.value)}>{priorities.map(p=><option key={p}>{p}</option>)}</select></td><td>{t.last_checked_at?new Date(t.last_checked_at).toLocaleString():"—"}</td><td>{t.consecutive_failures||0}</td></tr>)}</tbody></table>{!data.items.length&&<div className="empty small">No synced targets match.</div>}</div><div className="pager"><button disabled={page<=1} onClick={()=>setPage(x=>x-1)}>← Previous</button><span>Page {page} of {data.pages}</span><button disabled={page>=data.pages} onClick={()=>setPage(x=>x+1)}>Next →</button></div></div>}
